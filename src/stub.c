/* Empty stub compiled as libbluetooth_jni_orig.so.
 * Its only purpose is to give the shim a DT_NEEDED: libbluetooth_jni_orig.so
 * entry so that the real library (with that filename) gets loaded alongside
 * the shim at runtime, transitively bringing libc.so into the namespace. */